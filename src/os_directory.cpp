#include "os_directory.h"

const char * os_directory_running(char * path, size_t len)
{
    return path;
}

int os_directory_exist(const char * path)
{
    return 0;
}

int os_directroy_empty(const char * path)
{
    return 0;
}

int os_directory_remove(const char * path)
{
    return 0;
}

int os_directory_create(const char * path)
{
    return 0;
}
